Since this package only generates notion formula there is no restrictions to which package you could use.

This package integrates nicely with the [core](https://www.npmjs.com/package/@nishans/core) package, when you are trying create a schema.
