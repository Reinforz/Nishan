import { TAmount, TResult, TWay } from '../types';

export const amount_arr = [ 'single', 'multiple' ] as TAmount[];
export const result_arr = [ 'correct', 'incorrect' ] as TResult[];
export const way_arr = [ 'id', 'cb' ] as TWay[];
