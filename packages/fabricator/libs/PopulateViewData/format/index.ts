import { populateViewFormat } from './format';
import { populateViewFormatProperties } from './properties';

export const PopulateViewFormatData = {
	format: populateViewFormat,
	properties: populateViewFormatProperties
};
