name: Lint, Build and Test

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x, 14.x]
    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: yarn install
      - run: yarn test
      - uses: codecov/codecov-action@v1
        with:
          files: ./packages/core/coverage/lcov.info,./packages/cache/coverage/lcov.info
          flags: core,cache
      # - name: Uploading test coverage reports
      #   run: |
      #     chmod +x "${GITHUB_WORKSPACE}/scripts/upload_test_coverage.sh"
      #     "${GITHUB_WORKSPACE}/scripts/upload_test_coverage.sh"
